name: kaerel_utils
description: "A repo to handle kaerel utils"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  http: ^1.2.1
  flutter_json: ^0.0.3
  equatable: ^2.0.5
  kaerel_repository:
    path: ../kaerel_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
